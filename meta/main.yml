galaxy_info:
  namespace: vnode
  role_name: system
  author: Rogier Krieger
  description: Configure basic system settings (hostname, sysctl, kernel config)
  company: vNode.net
  issue_tracker_url: https://github.com/vnode/ansible-role-system/issues/
  license: MIT
  min_ansible_version: "2.9"

  platforms:
  - name: OpenBSD
    versions:
    - all
  - name: FreeBSD
    versions:
    - all

  galaxy_tags:
  - "system"
  - "hostname"
  - "sysctl"
  - "kernel"

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
